import 'package:gen_pdf/common.dart';

class Exporters extends StatelessWidget {
  const Exporters({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(child: Text("Exporters"));
  }
}
